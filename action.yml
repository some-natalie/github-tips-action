name: 'Tips Action'

description: 'Post a random tip to the specified Slack channel'

outputs:
  random-number:
    description: "Random tip"
    value: ${{ steps.random-tip.outputs.tip }}

runs:
  using: "composite"
  steps:
    - id: random-tip
      run: ${{ github.action_path }}/tip-picker.sh
      shell: bash